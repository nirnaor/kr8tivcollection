= simple_form_for user, html: { class: 'form-horizontal' } do |f|
  = f.input :email 
  = f.input :password  
  = f.input :password_confirmation  
  - unless defined? no_submit
    .form-actions
      = f.button :submit, class: 'btn-primary'
